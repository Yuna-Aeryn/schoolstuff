# include <stdio.h>
# include <stdlib.h> 

struct list { 
    char *name; 
    char *no; 

}
struct list *inp(struct list *n); 
struct list *nalloc(); 

int main() 
{ 
    struct list 
    man = NULL 
    p = *nalloc(); 
    p = inman); 
    printf("%s\n", p->name, p->90)
    free(p);
    return 0;
    
struct list *inp(strct list *n)
{ 
    char *name = (char - ) malloc (10); 
    char *no = char*malloc(10); 
    m = nalloc 
    scanf_s(%n, )
struct list ( int malloc )
}

}